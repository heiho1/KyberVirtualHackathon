/* eslint-disable object-shorthand */
/* eslint-disable func-names */
import Zaps from '../constants/Zaps';

const Spreads = [
  {
    id: 'lender',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'ETHMaximalist',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'moderatebull',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'doublebull',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'supersaver',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'unipooldai',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'unipoolmkr',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'unipoolsnx',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'unipoolseth',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'unipoolsai',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'bullSharpe',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'bearSharpe',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'moderate',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'moderateBear',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'longTermBull',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'shortTermBear',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'shortTermFlat',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'moderateDiversified',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'moderateFlat',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'hedgedBull',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'beginnerBull',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'beginnerBear',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'activeBear',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'activeBull',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'flatLands',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'riskyBear',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  },
  {
    id: 'riskyBull',
    spread: function(value) {
      console.log(`Spread for ${this.id} with ${value} is NYI`);
    }
  }
];

export default Spreads;
